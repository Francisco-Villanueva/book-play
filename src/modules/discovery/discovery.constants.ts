// La ventana por defecto. Tres días alcanza para "¿dónde juego hoy o mañana?"
// sin que la respuesta pese cientos de kilobytes.
export const DEFAULT_DAYS = 3;
export const MAX_DAYS = 7;

// Mismo tope que la búsqueda pública que ya existía.
export const MAX_BUSINESSES = 50;
export const DEFAULT_PAGE_SIZE = 20;

// El rail de "próximos turnos" de la ciudad. Más que esto no se scrollea.
export const DEFAULT_SLOTS = 20;
export const MAX_SLOTS = 50;

// Radio de la Tierra en km, para la distancia haversine.
export const EARTH_RADIUS_KM = 6371;
